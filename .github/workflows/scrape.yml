name: Scrape IPL Stats

on:
  schedule:
    - cron: '*/30 12-20 * * *' # every 30 mins from 12pm to 8pm UTC
  workflow_dispatch:          # manual trigger button in GitHub UI
  push:
    branches: [main]
    paths-ignore:
      - 'data.json'           # don't re-trigger when scraper commits data.json

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run scraper
        run: python scrape.py

      - name: Commit updated data.json
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data.json
          git diff --staged --quiet || (git commit -m "chore: update IPL stats [skip ci]" && git push)
